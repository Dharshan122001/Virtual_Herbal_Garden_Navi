trigger:
  branches:
    include:
      - fresh
  paths:
    include:
      - client/*
      - server/*
      - vhg-chart/*

variables:
  - group: vhg-secrets # Contains DB_PASSWORD and other secrets
  - name: dockerRegistryConn
    value: 'DockerHubConn' # Ensure this matches your Service Connection name
  - name: dockerUser
    value: 'dharshan3690'

stages:
# --- STAGE 1: Smart Detect & Resolve Tags ---
- stage: Detect_and_Resolve
  displayName: "0. Smart Detect"
  jobs:
  - job: ResolveTags
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - checkout: self
      fetchDepth: 0 

    - script: |
        # Get list of changed files
        PREV_COMMIT=$(git rev-parse HEAD~1 2>/dev/null || echo "HEAD")
        CHANGED_FILES=$(git diff --name-only $PREV_COMMIT HEAD)
        echo "Files modified: $CHANGED_FILES"
        
        # 1. Frontend Detection
        if echo "$CHANGED_FILES" | grep -q '^client/'; then
          echo "##vso[task.setvariable variable=f_changed;isOutput=true]true"
        else
          echo "##vso[task.setvariable variable=f_changed;isOutput=true]false"
        fi

        # 2. Plant Service Detection (Depends on server/plant or server/common)
        if echo "$CHANGED_FILES" | grep -qE "^(server/plant_service/|server/common/)"; then
          echo "##vso[task.setvariable variable=p_changed;isOutput=true]true"
        else
          echo "##vso[task.setvariable variable=p_changed;isOutput=true]false"
        fi

        # 3. Auth Service Detection
        if echo "$CHANGED_FILES" | grep -qE "^(server/auth_service/|server/common/)"; then
          echo "##vso[task.setvariable variable=au_changed;isOutput=true]true"
        else
          echo "##vso[task.setvariable variable=au_changed;isOutput=true]false"
        fi

        # 4. AI Service Detection
        if echo "$CHANGED_FILES" | grep -qE "^(server/ai_service/|server/common/)"; then
          echo "##vso[task.setvariable variable=ai_changed;isOutput=true]true"
        else
          echo "##vso[task.setvariable variable=ai_changed;isOutput=true]false"
        fi
      name: TagLogic

# --- STAGE 2: Build and Push (Conditional) ---
- stage: Build_and_Push
  displayName: "1. Build and Push"
  dependsOn: Detect_and_Resolve
  jobs:
  - job: DockerBuild
    pool:
      vmImage: 'ubuntu-latest'
    variables:
      f_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.f_changed'] ]
      p_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.p_changed'] ]
      au_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.au_changed'] ]
      ai_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.ai_changed'] ]
    steps:
    - task: Docker@2
      displayName: "Push Frontend"
      condition: eq(variables.f_upd, 'true')
      inputs:
        containerRegistry: '$(dockerRegistryConn)'
        repository: '$(dockerUser)/navi-frontend'
        command: 'buildAndPush'
        dockerfile: 'client/Dockerfile'
        tags: '$(Build.BuildId)'

    - task: Docker@2
      displayName: "Push Plant Service"
      condition: eq(variables.p_upd, 'true')
      inputs:
        containerRegistry: '$(dockerRegistryConn)'
        repository: '$(dockerUser)/plant-service'
        command: 'buildAndPush'
        dockerfile: 'server/plant_service/Dockerfile'
        tags: '$(Build.BuildId)'

    - task: Docker@2
      displayName: "Push Auth Service"
      condition: eq(variables.au_upd, 'true')
      inputs:
        containerRegistry: '$(dockerRegistryConn)'
        repository: '$(dockerUser)/auth-service'
        command: 'buildAndPush'
        dockerfile: 'server/auth_service/Dockerfile'
        tags: '$(Build.BuildId)'

    - task: Docker@2
      displayName: "Push AI Service"
      condition: eq(variables.ai_upd, 'true')
      inputs:
        containerRegistry: '$(dockerRegistryConn)'
        repository: '$(dockerUser)/ai-service'
        command: 'buildAndPush'
        dockerfile: 'server/ai_service/Dockerfile'
        tags: '$(Build.BuildId)'

# --- STAGE 3: GitOps Manifest Update ---
- stage: GitOps_Update
  displayName: "2. GitOps Sync"
  dependsOn: [Detect_and_Resolve, Build_and_Push]
  condition: succeeded()
  jobs:
  - job: UpdateManifest
    pool:
      vmImage: 'ubuntu-latest'
    variables:
      f_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.f_changed'] ]
      p_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.p_changed'] ]
      au_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.au_changed'] ]
      ai_upd: $[ stageDependencies.Detect_and_Resolve.ResolveTags.outputs['TagLogic.ai_changed'] ]
    steps:
    - checkout: self
      persistCredentials: true 

    - script: |
        # Install yq
        sudo wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/bin/yq && sudo chmod +x /usr/bin/yq

        # Update values.yaml only for components that were rebuilt
        if [ "$(f_upd)" == "true" ]; then yq -i ".frontend.tag = \"$(Build.BuildId)\"" vhg-chart/values.yaml; fi
        if [ "$(p_upd)" == "true" ]; then yq -i ".plant.tag = \"$(Build.BuildId)\"" vhg-chart/values.yaml; fi
        if [ "$(au_upd)" == "true" ]; then yq -i ".auth.tag = \"$(Build.BuildId)\"" vhg-chart/values.yaml; fi
        if [ "$(ai_upd)" == "true" ]; then yq -i ".ai.tag = \"$(Build.BuildId)\"" vhg-chart/values.yaml; fi

        # Git Push back to repo
        git config --global user.email "devops-pipeline@navikenz.com"
        git config --global user.name "Azure DevOps Pipeline"
        
        # Only commit if changes were actually made to values.yaml
        if git diff --exit-code vhg-chart/values.yaml; then
          echo "No manifest changes to commit."
        else
          git add vhg-chart/values.yaml
          git commit -m "GitOps: update image tags to $(Build.BuildId) [skip ci]"
          git push origin HEAD:fresh
        fi
      displayName: "Update vhg-chart/values.yaml and Sync Git"