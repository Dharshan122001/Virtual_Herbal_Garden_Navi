in the manager credentials 

github : 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

cd K8s && terraform output -raw cluster_name >/dev/null && az aks get-credentials --resource-group Darshan.k_lean_rg --name vhg-aks --file - | base64 | tr -d '\n'
secret text : aks-kubeconfig : 

dockerhub-creds

useraname and password. : kdharshan3690@gmail.com : Vinu@9945385205


terraform apply -target=module.azure_infra -var-file="secrets.tfvars"
terraform apply -var-file="secrets.tfvars"

whenever you do terraform destroy and then apply copy the code from this 
az aks get-credentials --resource-group Darshan.k_lean_rg --name vhg-aks --file - | base64 | tr -d '\n' | pbcopy
and paste it is aks-kubeconfig